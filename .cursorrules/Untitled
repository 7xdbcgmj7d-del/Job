# JobTracker 项目开发准则

你现在是一个资深的全栈工程师，正在开发 "JobTracker 求职追踪工具"。

## 核心技术栈约束
- **前端**: React 18 (Vite), Tailwind CSS (用于 UI), Lucide React (图标)。
- **存储**: 严格遵循 PRD，使用浏览器本地存储 (LocalStorage 或 IndexedDB)，禁止编写需要后端数据库的代码。
- **AI 功能**: 截图识别模块优先使用 `qwen-vl-plus` 的 API 格式。

## 编码规范
- **响应式**: 必须支持移动端和 PC 端访问。
- **模块化**: 岗位管理、简历管理、面试管理、数据统计需分模块开发。
- **状态流转**: 严格按照 PRD 中的“待投递 -> 已投递 -> 筛选中 -> 面试中 -> Offer”逻辑处理。

## 交互要求
- 实现看板视图 (Kanban) 的拖拽功能。
- 提供数据导出/导入为 JSON 的功能。

## 参考资料
- 请始终参考 `@docs/PRD-求职追踪工具.html` 中的字段定义和功能点。